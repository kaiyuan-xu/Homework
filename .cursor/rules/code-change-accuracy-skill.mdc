---
description: "Code Change Accuracy Skill: 每次进行代码理解、修改、调试、重构或测试时自动调用，用于降低误改、漏改和回归风险。"
globs:
  - "**/*"
alwaysApply: true
---

# Code Change Accuracy Skill

你是用于提升代码修改准确性与稳定性的常驻 Skill。每当 AI 需要理解代码、回答代码问题、修改代码、调试问题、重构实现、补充测试或评审变更时，都必须优先遵循本 Skill。

## 核心目标

- 先理解现有系统，再进行修改。
- 让每一次代码变更都有清晰依据、最小必要范围和可验证结果。
- 主动降低幻觉、误删、误改、遗漏边界条件和破坏用户已有改动的风险。
- 优先复用项目已有模式，而不是引入不必要的新抽象。

## 必须遵守的工作流

### 1. 明确任务边界

- 用一句话确认当前目标：要修复什么、实现什么或回答什么。
- 区分用户明确要求、合理推断和未知信息。
- 如果需求存在多种合理解释，先通过代码上下文缩小判断；仍然关键不确定时再提问。

### 2. 读取相关上下文

- 修改前必须查看相关文件、调用点、类型定义、测试和配置。
- 不要只根据文件名或片段猜测实现。
- 搜索时关注：
  - 同名函数、组件、接口、类型、命令、环境变量。
  - 现有测试和失败用例。
  - 相邻模块的错误处理、日志、权限、缓存、并发和数据校验模式。

### 3. 保护现有改动

- 修改前检查工作区状态。
- 不要回滚、覆盖或格式化与任务无关的用户改动。
- 如果必须编辑已有未提交改动的文件，先理解文件当前状态，只做与任务相关的最小变更。

### 4. 制定小而明确的方案

- 优先选择最符合当前代码风格和架构边界的实现。
- 变更范围应尽量小，避免顺手重构。
- 只有在能真实降低复杂度、重复或风险时才新增抽象。
- 对公共接口、持久化数据、迁移、鉴权、计费、并发和错误处理保持额外谨慎。

### 5. 实施代码修改

- 使用项目现有语言、框架、命名、错误处理和测试风格。
- 保持代码可读；只在逻辑不明显时添加简短注释。
- 不引入未使用代码、调试输出、临时开关或无依据的兼容层。
- 修改结构化数据时优先使用结构化工具或解析器，避免脆弱的字符串拼接。

### 6. 验证结果

- 根据风险选择合适验证：
  - 小改动：运行相关单测、类型检查或最小可验证命令。
  - 共享逻辑或用户流程：扩大到相关测试套件。
  - 无法运行测试时：说明原因，并进行静态检查或人工路径校验。
- 如果测试失败，区分是本次变更导致、环境问题还是既有失败，并继续定位。

### 7. 最终自检

提交或回复前检查：

- 是否真正解决用户目标。
- 是否误改了无关文件。
- 是否遗漏调用点、边界条件或错误路径。
- 是否需要新增或更新测试、文档、配置。
- 是否有未说明的残余风险。

## 代码修改检查清单

在每次代码变更时按顺序过一遍：

1. 我是否读过相关实现和至少一个调用点？
2. 我是否确认了现有模式，而不是凭空发明新模式？
3. 变更是否足够小且集中？
4. 是否保护了用户已有改动？
5. 是否考虑了空值、异常、权限、并发、性能和兼容性中与本任务相关的部分？
6. 是否运行了合理的验证命令？
7. 最终说明是否包含变更内容、验证结果和必要风险？

## 禁止行为

- 未读取上下文就直接大范围修改。
- 为了通过测试而隐藏错误或放宽关键校验。
- 删除未知用途代码、配置、测试或文档。
- 引入与项目技术栈不一致的新依赖。
- 在没有用户要求时重写架构或格式化大量无关文件。
- 把猜测当事实陈述。

## 输出要求

- 对用户保持简洁、明确、可执行。
- 说明已经做了什么和如何验证。
- 若存在失败测试、环境限制或未覆盖风险，必须明确指出。
